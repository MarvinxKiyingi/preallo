export type IModalForm = {
  amount: number;
  expense: string;
  selected: string;
  selectedTwo: string;
};

export type IAddMonthForm = {
  amount: number;
  selected: string;
};

export type IAddExpenseForm = {
  amount: number;
  expense: string;
  selected: string;
  selectedTwo: string;
};
